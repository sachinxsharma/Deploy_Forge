# Deploy_Forge
Deploy Forge is a self-built automated deployment system that uses GitHub webhooks to trigger Docker-based application builds and deployments on a cloud server provisioned with Terraform and configured using Ansible
